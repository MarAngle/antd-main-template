
----
## 考虑
- update阶段load算作何种状态=>
    理论上更新是对加载数据的更新操作，此时加载数据一直是加载完成的状态比较符合常理
- update考虑扩展到多个update协调=>
    后期考虑，将问题复杂化且使用次数过少
- BaseData: 鉴于CLASS生成时间段的问题,created生命周期触发于父类创建事件节点，此时子类创建未完成，等待优化方案=>
    暂时可以考虑在链式类继承时额外传递字段判断，最终在子类中调用生命周期函数
- DictionaryData: 列表数据暂时无法设置特殊TITLE，找到解决方案后删除
- DictionaryData: order实现方案暂未有比较好的思路，待定中...0821
- ParentData: 归属函数，暂时实现到单父类形式，根据情况考虑是否存在多个归属的情况
- DList: 树形结构的属性是否固定设置为array
- list数据全赋值导致的更新操作覆盖行为的解决方案...考虑vue.set实现数据的绑定操作%重要!!!

----
## 待定
- 唯一识别符生成器打乱时间戳顺序
- 对象复杂结构的列表展示逻辑待定

----
## 未完成
- 树结构数据类型解析=>实现数据格式化,列表展开和树形结构解析
- 统一的格式判断函数实现，字符串数字电话等
- 添加弹窗模块mixin，实现弹窗的统一处理
- 修改增加删除后的本地数据同步处理逻辑和重新拉取数据逻辑优化
- form表单加载中状态判断，文件上传时限制操作等
- 树形结构扩展
- 列表实现本地分页，考虑从数据池结构实现
- 添加全局监控事件，可由BD自行触发全局监控到函数操作
- 模块组件使用install方法实现可变加载
- 生命周期函数实现once，immediate

----
## 进行中

----
## 已完成
- 关于editdata的结构考虑优化精简，通过JSX语法实现逻辑的优化和代码的精简
- DictionaryList: getFormData根据传值和Type进行初始化判断
- FormModelView中的label值更改为ditem.label[type]，避免出现edit复用情况下的名称不同问题
- 实现列表定制-样式-悬停，事件等
- TableList组件的JSX化

