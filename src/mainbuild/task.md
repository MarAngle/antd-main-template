
----
## 考虑
- update阶段load算作何种状态=>
    理论上更新是对加载数据的更新操作，此时加载数据一直是加载完成的状态比较符合常理
- update考虑扩展到多个update协调=>
    后期考虑，将问题复杂化且使用次数过少
- DictionaryData: order实现方案暂未有比较好的思路，待定中...0821
- ParentData: 归属函数，暂时实现到单父类形式，根据情况考虑是否存在多个归属的情况
- DList: 树形结构的属性是否固定设置为array
- list数据全赋值导致的更新操作覆盖行为的解决方案...考虑vue.set实现数据的绑定操作%重要!!!
- 分页器的宽度与table的宽度自适应问题，实现更好的界面操作=>思路是tabel和page单独处理宽度问题，整个页面的最小宽度限定
- 模块组件使用install方法实现可变加载=>模块加载的冗余加载存在问题，考虑通过额外传值实现加载判断避免冗余加载，后期考虑

----
## 待定
- 唯一识别符生成器打乱时间戳顺序
- 对象复杂结构的列表展示逻辑待定
- 添加弹窗模块mixin，实现弹窗的统一处理

----
## 未完成
- 统一的格式判断函数实现，字符串数字电话等
- 修改增加删除后的本地数据同步处理逻辑优化
- form表单加载中状态判断，文件上传时限制操作等
- 列表实现本地分页，考虑从数据池结构实现
- 根据moment类型优化时间相关函数
- 插件化

----
## 进行中
- 树结构数据类型解析=>实现数据格式化,列表展开和树形结构解析
- 准备在模块加载时进行生命周期的添加，实现统一处理:统一处理适时实现
- install模块避免冲突需要设置单独的ID

----
## 已完成
- 修改生命周期函数初始化传值，实现对created生命周期的回调操作=>created生命周期回调传值this
- created生命周期触发通过当前类名判断实现各级依赖的创建事件触发
- data的初始化赋值
- 全局类使用SimpleData作为基底
- 考虑使用Map替换对象，替换属性变更较多的配置项
- 重新拉取数据逻辑优化
- INFO数据
- TREE数据
- 生命周期回调顺序操作=>基本顺序操作，理论上index值会被后期的回调进行其他操作因此仅作参考
- 添加字典检索功能
- 关于editdata的结构考虑优化精简，通过JSX语法实现逻辑的优化和代码的精简
- DictionaryList: getFormData根据传值和Type进行初始化判断
- FormModelView中的label值更改为ditem.label[type]，避免出现edit复用情况下的名称不同问题
- 实现列表定制-样式-悬停，事件等
- TableList组件的JSX化
- 分页器插槽的多设置=>实现更高的定制化
- 生命周期函数实现once，immediate
- 文件上传插件的数据重复赋值问题
- 优化生命周期类，实现自定义生命周期的触发和监控，更好的对不同分页数据进行监控，并可创建全局唯一变量进行生命周期的操作=>等同意event BUS

